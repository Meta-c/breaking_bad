import 'package:flutter/material.dart';

class CharactersScreens extends StatelessWidget {
  const CharactersScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Breaking BOIII'),
    );
  }
}
